import React from "react";
import { DeliveriesWrapper } from "./DeliveriesStyled";

const Deliveries = (props) => {
  return <DeliveriesWrapper>Deliveries</DeliveriesWrapper>;
};

export default Deliveries;
