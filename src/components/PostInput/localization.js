import { defineMessages } from "react-intl";

export default defineMessages({
  defaultText: {
    id: "post.input.defaultText",
    defaultMessage: "Say hi to your neighbors!",
  },
  publish: {
    id: "post.input.publish",
    defaultMessage: "Publish",
  },
});
